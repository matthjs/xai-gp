# DSPP model configuration

type: "DSPPModel"
gp_mode: "DSPP"
num_inducing_points: 500
beta: 0.05 # Add beta parameter

# Same as GP for now
hidden_layers:
  - output_dims: 4
    mean_type: "linear"
  - output_dims: 4
    mean_type: "linear"
  - output_dims: null
    mean_type: "constant"
