# DSPP model configuration

type: "DSPPModel"
gp_mode: "DSPP"
num_inducing_points: 128
beta: 0.05 # Add beta parameter

# Same as GP for now
hidden_layers:
  - output_dims: 2
    mean_type: "constant"
