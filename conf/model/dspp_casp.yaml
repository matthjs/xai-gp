# DSPP model configuration

type: "DSPPModel"
gp_mode: "DSPP"
num_inducing_points: 50
beta: 1 # Add beta parameter

# Same as GP for now
hidden_layers:
  - output_dims: null
    mean_type: "constant"
